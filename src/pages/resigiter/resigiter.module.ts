import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ResigiterPage } from './resigiter';

@NgModule({
  declarations: [
    ResigiterPage,
  ],
  imports: [
    IonicPageModule.forChild(ResigiterPage),
  ],
})
export class ResigiterPageModule {}
